__pycache__/
*.pyc
*.pyo
.DS_Store
._*
.env
*.env
# llm-wiki runtime workspace ($WIKI_DIR, defaults to ./wiki/) — generated state.
wiki/
# Eval wikis are source: the diagnostic layer the skill loop reads and writes.
!evals/*/wiki/
CLAUDE.md
.firecrawl/

# Gemini CLI runtime artifacts (created by the weekly-skill-refresh workflow);
# config is generated from the workflow's `settings` input, not source.
.gemini/
gemini-artifacts/
